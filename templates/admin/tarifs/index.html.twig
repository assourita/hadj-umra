{% extends 'layouts/admin_dashboard.html.twig' %}

{% block title %}Gestion des Tarifs - Administration{% endblock %}

{% block body %}
<div class="page-header">
    <h1>💰 Gestion des Tarifs</h1>
    <p>Gérez les tarifs pour tous les départs</p>
</div>

<div class="content">
    <div class="actions-bar">
        <p class="info-text">Les tarifs sont créés automatiquement lors de la création des départs.</p>
    </div>

    {% if tarifs is empty %}
        <div class="empty-state">
            <div class="empty-icon">💰</div>
            <h2>Aucun tarif configuré</h2>
            <p>Commencez par créer votre premier tarif.</p>
            <a href="{{ path('app_admin_tarif_new') }}" class="btn btn-primary">
                Créer un tarif
            </a>
        </div>
    {% else %}
        <div class="tarifs-table">
            <table>
                <thead>
                    <tr>
                        <th>Départ</th>
                        <th>Type de chambre</th>
                        <th>Prix</th>
                        <th>Devise</th>
                        <th>Statut</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for tarif in tarifs %}
                        <tr>
                            <td>
                                <div class="depart-info">
                                    <strong>{{ tarif.depart.package.titre }}</strong>
                                    <span class="depart-date">{{ tarif.depart.dateDepart|date('d/m/Y') }}</span>
                                </div>
                            </td>
                            <td>
                                <span class="room-type">{{ tarif.typeChambre|title }}</span>
                            </td>
                            <td>
                                <span class="price">{{ tarif.prix|number_format(0, ',', ' ') }}</span>
                            </td>
                            <td>
                                <span class="currency">{{ tarif.devise }}</span>
                            </td>
                            <td>
                                {% if tarif.isActive %}
                                    <span class="status-badge active">✅ Actif</span>
                                {% else %}
                                    <span class="status-badge inactive">❌ Inactif</span>
                                {% endif %}
                            </td>
                            <td>
                                <div class="actions">
                                    <a href="{{ path('app_admin_tarif_edit', {'id': tarif.id}) }}" class="btn btn-secondary btn-small">
                                        ✏️ Modifier
                                    </a>
                                    <form method="POST" action="{{ path('app_admin_tarif_delete', {'id': tarif.id}) }}" 
                                          style="display: inline;" 
                                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce tarif ?')">
                                        <button type="submit" class="btn btn-danger btn-small">
                                            🗑️ Supprimer
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
</div>

<style>
.page-header {
    margin-bottom: 2rem;
}

.page-header h1 {
    margin: 0 0 0.5rem 0;
    color: #1f2937;
    font-size: 2rem;
}

.page-header p {
    margin: 0;
    color: #6b7280;
    font-size: 1.125rem;
}

.content {
    background: white;
    border-radius: 1rem;
    padding: 2rem;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
}

.actions-bar {
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid #f3f4f6;
}

.info-text {
    margin: 0;
    color: #6b7280;
    font-size: 0.875rem;
    font-style: italic;
}

.empty-state {
    text-align: center;
    padding: 4rem 2rem;
}

.empty-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
}

.empty-state h2 {
    margin: 0 0 1rem 0;
    color: #1f2937;
    font-size: 1.5rem;
}

.empty-state p {
    margin: 0 0 2rem 0;
    color: #6b7280;
    font-size: 1.125rem;
}

.tarifs-table {
    overflow-x: auto;
}

.tarifs-table table {
    width: 100%;
    border-collapse: collapse;
}

.tarifs-table th {
    text-align: left;
    padding: 1rem;
    background: #f9fafb;
    color: #374151;
    font-weight: 600;
    font-size: 0.875rem;
    border-bottom: 1px solid #e5e7eb;
}

.tarifs-table td {
    padding: 1rem;
    border-bottom: 1px solid #f3f4f6;
    color: #1f2937;
}

.tarifs-table tr:hover {
    background: #f9fafb;
}

.depart-info {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
}

.depart-info strong {
    color: #1f2937;
}

.depart-date {
    font-size: 0.875rem;
    color: #6b7280;
}

.room-type {
    background: #e0f2fe;
    color: #0277bd;
    padding: 0.25rem 0.75rem;
    border-radius: 2rem;
    font-size: 0.875rem;
    font-weight: 600;
}

.price {
    font-weight: 700;
    color: #059669;
    font-size: 1.125rem;
}

.currency {
    background: #f3f4f6;
    color: #374151;
    padding: 0.25rem 0.5rem;
    border-radius: 0.25rem;
    font-size: 0.875rem;
    font-weight: 600;
}

.status-badge {
    padding: 0.25rem 0.75rem;
    border-radius: 2rem;
    font-size: 0.75rem;
    font-weight: 600;
}

.status-badge.active {
    background: #dcfce7;
    color: #166534;
}

.status-badge.inactive {
    background: #fee2e2;
    color: #991b1b;
}

.actions {
    display: flex;
    gap: 0.5rem;
}

.btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.875rem;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
}

.btn-primary {
    background: #3b82f6;
    color: white;
}

.btn-primary:hover {
    background: #2563eb;
    transform: translateY(-1px);
}

.btn-secondary {
    background: #6b7280;
    color: white;
}

.btn-secondary:hover {
    background: #4b5563;
    transform: translateY(-1px);
}

.btn-danger {
    background: #ef4444;
    color: white;
}

.btn-danger:hover {
    background: #dc2626;
    transform: translateY(-1px);
}

.btn-small {
    padding: 0.5rem 1rem;
    font-size: 0.75rem;
}

@media (max-width: 768px) {
    .tarifs-table {
        font-size: 0.875rem;
    }
    
    .tarifs-table th,
    .tarifs-table td {
        padding: 0.75rem 0.5rem;
    }
    
    .actions {
        flex-direction: column;
    }
}
</style>
{% endblock %}
