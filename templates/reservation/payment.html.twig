{% extends 'base.html.twig' %}

{% block title %}Paiement - Réservation #{{ reservation.id }} - Dèmè Travel{% endblock %}

{% block body %}
<div class="payment-page">
    <!-- Navigation par étapes -->
    <div class="steps-navigation">
        <div class="step-item completed">
            <div class="step-number">1</div>
            <div class="step-content">
                <h4>Informations voyage</h4>
                <p>Type de chambre et nombre de pèlerins</p>
            </div>
        </div>
        <div class="step-connector"></div>
        <div class="step-item completed">
            <div class="step-number">2</div>
            <div class="step-content">
                <h4>Détails pèlerins</h4>
                <p>Informations personnelles des voyageurs</p>
            </div>
        </div>
        <div class="step-connector"></div>
        <div class="step-item active">
            <div class="step-number">3</div>
            <div class="step-content">
                <h4>Paiement</h4>
                <p>Finalisation de la réservation</p>
            </div>
        </div>
    </div>

    <!-- Liens de navigation -->
    <div class="step-navigation-links">
        <a href="{{ path('app_reservation_step1', {'departId': reservation.depart.id}) }}" class="btn btn-outline">
            ← Retour à l'étape 1
        </a>
        <a href="{{ path('app_reservation_step2') }}" class="btn btn-outline">
            ← Retour à l'étape 2
        </a>
    </div>

    <div class="page-header">
        <h1>💳 Paiement</h1>
        <p>Réservation #{{ reservation.id }} - {{ reservation.depart.package.titre }}</p>
    </div>

    <div class="payment-content">
        <div class="payment-grid">
            <!-- Détails de la réservation -->
            <div class="reservation-details">
                <h2>📋 Détails de la réservation</h2>
                
                <div class="reservation-card">
                    <div class="reservation-header">
                        <h3>{{ reservation.depart.package.titre }}</h3>
                        <span class="reservation-id">#{{ reservation.id }}</span>
                    </div>
                    
                    <div class="reservation-info">
                        <div class="info-item">
                            <span class="info-label">Date de départ :</span>
                            <span class="info-value">{{ reservation.depart.dateDepart|date('d/m/Y') }}</span>
                        </div>
                        
                        <div class="info-item">
                            <span class="info-label">Type de chambre :</span>
                            <span class="info-value">{{ reservation.typeChambre|title }}</span>
                        </div>
                        
                        <div class="info-item">
                            <span class="info-label">Nombre de pèlerins :</span>
                            <span class="info-value">{{ reservation.nbPelerins }}</span>
                        </div>
                        
                        <div class="info-item">
                            <span class="info-label">Statut :</span>
                            <span class="info-value">
                                <span class="status-badge {{ reservation.statut }}">
                                    {{ reservation.statut|title }}
                                </span>
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Liste des pèlerins -->
                <div class="pilgrims-list">
                    <h3>👥 Pèlerins</h3>
                    {% for pelerin in reservation.pelerins %}
                        <div class="pilgrim-item">
                            <div class="pilgrim-number">{{ loop.index }}</div>
                            <div class="pilgrim-info">
                                <div class="pilgrim-name">{{ pelerin.prenom }} {{ pelerin.nom }}</div>
                                <div class="pilgrim-details">
                                    {{ pelerin.dateNaissance|date('d/m/Y') }} • {{ pelerin.sexe == 'M' ? 'Masculin' : 'Féminin' }}
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>

            <!-- Formulaire de paiement -->
            <div class="payment-form">
                <h2>💳 Informations de paiement</h2>
                
                <form method="POST" action="{{ path('app_reservation_payment', {'id': reservation.id}) }}" class="payment-form-fields">
                    <div class="payment-summary">
                        <div class="summary-item">
                            <span>Montant total :</span>
                            <span class="total-amount">{{ reservation.total|number_format(0, ',', ' ') }} FCFA</span>
                        </div>
                        
                        <div class="summary-item">
                            <span>Acompte (30%) :</span>
                            <span>{{ (reservation.total * 0.3)|number_format(0, ',', ' ') }} FCFA</span>
                        </div>
                        
                        <div class="summary-item">
                            <span>Solde (70%) :</span>
                            <span>{{ (reservation.total * 0.7)|number_format(0, ',', ' ') }} FCFA</span>
                        </div>
                    </div>

                    <div class="payment-methods">
                        <h3>Méthodes de paiement acceptées</h3>
                        
                        <div class="payment-options">
                            <div class="payment-option">
                                <input type="radio" id="card" name="payment_method" value="card" checked class="payment-radio">
                                <label for="card" class="payment-label">
                                    <div class="payment-icon">💳</div>
                                    <div class="payment-info">
                                        <div class="payment-name">Carte bancaire</div>
                                        <div class="payment-description">Visa, Mastercard, American Express</div>
                                    </div>
                                </label>
                            </div>
                            
                            <div class="payment-option">
                                <input type="radio" id="transfer" name="payment_method" value="transfer" class="payment-radio">
                                <label for="transfer" class="payment-label">
                                    <div class="payment-icon">🏦</div>
                                    <div class="payment-info">
                                        <div class="payment-name">Virement bancaire</div>
                                        <div class="payment-description">Transfert direct vers notre compte</div>
                                    </div>
                                </label>
                            </div>
                            
                            <div class="payment-option">
                                <input type="radio" id="mobile" name="payment_method" value="mobile" class="payment-radio">
                                <label for="mobile" class="payment-label">
                                    <div class="payment-icon">📱</div>
                                    <div class="payment-info">
                                        <div class="payment-name">Paiement mobile</div>
                                        <div class="payment-description">Orange Money, MTN Mobile Money</div>
                                    </div>
                                </label>
                            </div>
                        </div>
                    </div>

                    <!-- Formulaire de carte bancaire -->
                    <div class="card-form" id="card-form">
                        <h3>Informations de la carte</h3>
                        
                        <div class="form-group">
                            <label for="card_number">Numéro de carte *</label>
                            <input type="text" id="card_number" name="card_number" placeholder="1234 5678 9012 3456" class="form-control">
                        </div>
                        
                        <div class="form-row">
                            <div class="form-group">
                                <label for="expiry_date">Date d'expiration *</label>
                                <input type="text" id="expiry_date" name="expiry_date" placeholder="MM/AA" class="form-control">
                            </div>
                            <div class="form-group">
                                <label for="cvv">CVV *</label>
                                <input type="text" id="cvv" name="cvv" placeholder="123" class="form-control">
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label for="cardholder_name">Nom du titulaire *</label>
                            <input type="text" id="cardholder_name" name="cardholder_name" placeholder="Nom comme sur la carte" class="form-control">
                        </div>
                        
                        <div class="form-group">
                            <label for="billing_address">Adresse de facturation</label>
                            <textarea id="billing_address" name="billing_address" rows="3" class="form-control" placeholder="Adresse complète pour la facturation"></textarea>
                        </div>
                    </div>

                    <!-- Informations de virement -->
                    <div class="transfer-info" id="transfer-info" style="display: none;">
                        <h3>Informations de virement</h3>
                        <div class="bank-details">
                            <div class="bank-item">
                                <span class="bank-label">Banque :</span>
                                <span class="bank-value">Banque de l'Habitat du Mali</span>
                            </div>
                            <div class="bank-item">
                                <span class="bank-label">Compte :</span>
                                <span class="bank-value">1234567890</span>
                            </div>
                            <div class="bank-item">
                                <span class="bank-label">IBAN :</span>
                                <span class="bank-value">ML123456789012345678901234</span>
                            </div>
                            <div class="bank-item">
                                <span class="bank-label">Bénéficiaire :</span>
                                <span class="bank-value">Dèmè Travel SARL</span>
                            </div>
                            <div class="bank-item">
                                <span class="bank-label">Référence :</span>
                                <span class="bank-value">RES-{{ reservation.id }}</span>
                            </div>
                        </div>
                    </div>

                    <!-- Informations mobile money -->
                    <div class="mobile-info" id="mobile-info" style="display: none;">
                        <h3>Paiement mobile</h3>
                        <div class="mobile-options">
                            <div class="mobile-option">
                                <input type="radio" id="orange" name="mobile_provider" value="orange" class="mobile-radio">
                                <label for="orange" class="mobile-label">
                                    <span class="mobile-icon">🟠</span>
                                    <span>Orange Money</span>
                                </label>
                            </div>
                            <div class="mobile-option">
                                <input type="radio" id="mtn" name="mobile_provider" value="mtn" class="mobile-radio">
                                <label for="mtn" class="mobile-label">
                                    <span class="mobile-icon">🟡</span>
                                    <span>MTN Mobile Money</span>
                                </label>
                            </div>
                        </div>
                        <div class="mobile-number">
                            <label for="mobile_number">Numéro de téléphone *</label>
                            <input type="tel" id="mobile_number" name="mobile_number" placeholder="+223 XX XX XX XX" class="form-control">
                        </div>
                    </div>

                    <div class="payment-actions">
                        <button type="submit" class="btn btn-primary btn-large" id="pay-button">
                            💳 Payer maintenant
                        </button>
                        <a href="{{ path('app_client_reservations') }}" class="btn btn-secondary">
                            Annuler
                        </a>
                    </div>
                </form>

                <!-- Les boutons sont maintenant dans le formulaire de carte bancaire -->
            </div>
        </div>

        <!-- Sécurité -->
        <div class="security-section">
            <h2>🔒 Sécurité des paiements</h2>
            <div class="security-grid">
                <div class="security-item">
                    <div class="security-icon">🔐</div>
                    <div class="security-content">
                        <h3>Paiement sécurisé</h3>
                        <p>Tous nos paiements sont protégés par un cryptage SSL 256 bits.</p>
                    </div>
                </div>
                
                <div class="security-item">
                    <div class="security-icon">🛡️</div>
                    <div class="security-content">
                        <h3>Données protégées</h3>
                        <p>Vos informations de paiement ne sont jamais stockées sur nos serveurs.</p>
                    </div>
                </div>
                
                <div class="security-item">
                    <div class="security-icon">✅</div>
                    <div class="security-content">
                        <h3>Confirmation immédiate</h3>
                        <p>Vous recevrez une confirmation de paiement par email.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.payment-page {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
}

.page-header {
    text-align: center;
    margin-bottom: 3rem;
    padding: 2rem;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: white;
    border-radius: 1rem;
}

.page-header h1 {
    margin: 0 0 1rem 0;
    font-size: 2.5rem;
}

.page-header p {
    margin: 0;
    font-size: 1.1rem;
    opacity: 0.9;
}

/* Navigation par étapes */
.steps-navigation {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 3rem;
    padding: 2rem;
    background: white;
    border-radius: 1rem;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
}

.step-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    border-radius: 0.5rem;
    transition: all 0.3s ease;
}

.step-item.completed {
    background: #f0f9ff;
    border: 2px solid #0ea5e9;
}

.step-item.active {
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: white;
}

.step-number {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 1.2rem;
}

.step-item.completed .step-number {
    background: #0ea5e9;
    color: white;
}

.step-item.active .step-number {
    background: white;
    color: var(--color-primary);
}

.step-content h4 {
    margin: 0 0 0.25rem 0;
    font-size: 1rem;
}

.step-content p {
    margin: 0;
    font-size: 0.875rem;
    opacity: 0.8;
}

.step-connector {
    width: 60px;
    height: 2px;
    background: #e5e7eb;
    margin: 0 1rem;
}

.step-item.completed + .step-connector {
    background: #0ea5e9;
}

.payment-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    margin-bottom: 3rem;
}

.reservation-details h2,
.payment-form h2 {
    margin: 0 0 2rem 0;
    color: var(--color-primary);
    font-size: 1.5rem;
}

.reservation-card {
    background: white;
    border-radius: 1rem;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    overflow: hidden;
    margin-bottom: 2rem;
}

.reservation-header {
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: white;
    padding: 1.5rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.reservation-header h3 {
    margin: 0;
    font-size: 1.25rem;
}

.reservation-id {
    background: rgba(255,255,255,0.2);
    padding: 0.25rem 0.75rem;
    border-radius: 1rem;
    font-size: 0.875rem;
}

.reservation-info {
    padding: 2rem;
}

.info-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem 0;
    border-bottom: 1px solid #f0f0f0;
}

.info-item:last-child {
    border-bottom: none;
}

.info-label {
    color: var(--color-gray);
}

.info-value {
    color: var(--color-primary);
    font-weight: 500;
}

.status-badge {
    padding: 0.25rem 0.75rem;
    border-radius: 1rem;
    font-size: 0.875rem;
    font-weight: 600;
}

.status-badge.en_attente_paiement {
    background: #fff3cd;
    color: #856404;
}

.pilgrims-list {
    background: white;
    padding: 2rem;
    border-radius: 1rem;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
}

.pilgrims-list h3 {
    margin: 0 0 1.5rem 0;
    color: var(--color-primary);
}

.pilgrim-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 0;
    border-bottom: 1px solid #f0f0f0;
}

.pilgrim-item:last-child {
    border-bottom: none;
}

.pilgrim-number {
    width: 2rem;
    height: 2rem;
    background: var(--color-primary);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 0.875rem;
}

.pilgrim-name {
    font-weight: 600;
    color: var(--color-primary);
    margin-bottom: 0.25rem;
}

.pilgrim-details {
    color: var(--color-gray);
    font-size: 0.875rem;
}

.payment-form {
    background: white;
    padding: 2rem;
    border-radius: 1rem;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
}

.payment-summary {
    background: #f8f9fa;
    padding: 1.5rem;
    border-radius: 0.5rem;
    margin-bottom: 2rem;
}

.summary-item {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.75rem;
    color: var(--color-gray);
}

.summary-item:last-child {
    margin-bottom: 0;
}

.total-amount {
    font-weight: 700;
    color: var(--color-primary);
    font-size: 1.1rem;
}

.payment-methods {
    margin-bottom: 2rem;
}

.payment-methods h3 {
    margin: 0 0 1rem 0;
    color: var(--color-primary);
}

.payment-options {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.payment-option {
    position: relative;
}

.payment-radio {
    display: none;
}

.payment-label {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    border: 2px solid #e9ecef;
    border-radius: 0.5rem;
    cursor: pointer;
    transition: all 0.3s ease;
}

.payment-radio:checked + .payment-label {
    border-color: var(--color-primary);
    background: rgba(139, 69, 19, 0.05);
}

.payment-icon {
    font-size: 1.5rem;
    min-width: 2rem;
}

.payment-name {
    font-weight: 600;
    color: var(--color-primary);
    margin-bottom: 0.25rem;
}

.payment-description {
    color: var(--color-gray);
    font-size: 0.875rem;
}

.card-form,
.transfer-info,
.mobile-info {
    margin-bottom: 2rem;
}

.card-form h3,
.transfer-info h3,
.mobile-info h3 {
    margin: 0 0 1rem 0;
    color: var(--color-primary);
}

.form-group {
    margin-bottom: 1.5rem;
}

.form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: var(--color-gray);
}

.form-control {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid #d1d5db;
    border-radius: 0.5rem;
    font-size: 1rem;
    transition: border-color 0.3s ease;
}

.form-control:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(139, 69, 19, 0.1);
}

.bank-details {
    background: #f8f9fa;
    padding: 1.5rem;
    border-radius: 0.5rem;
}

.bank-item {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.75rem;
    padding: 0.5rem 0;
    border-bottom: 1px solid #dee2e6;
}

.bank-item:last-child {
    border-bottom: none;
    margin-bottom: 0;
}

.bank-label {
    font-weight: 600;
    color: var(--color-gray);
}

.bank-value {
    color: var(--color-primary);
    font-weight: 500;
}

.mobile-options {
    display: flex;
    gap: 1rem;
    margin-bottom: 1rem;
}

.mobile-option {
    flex: 1;
}

.mobile-radio {
    display: none;
}

.mobile-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem;
    border: 2px solid #e9ecef;
    border-radius: 0.5rem;
    cursor: pointer;
    transition: all 0.3s ease;
    justify-content: center;
}

.mobile-radio:checked + .mobile-label {
    border-color: var(--color-primary);
    background: rgba(139, 69, 19, 0.05);
}

.mobile-icon {
    font-size: 1.25rem;
}

.payment-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
}

.btn-large {
    padding: 1rem 2rem;
    font-size: 1.1rem;
    font-weight: 600;
}

.security-section {
    background: white;
    padding: 2rem;
    border-radius: 1rem;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
}

.security-section h2 {
    margin: 0 0 2rem 0;
    color: var(--color-primary);
    font-size: 1.5rem;
    text-align: center;
}

.security-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
}

.security-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1rem;
    background: #f8f9fa;
    border-radius: 0.5rem;
}

.security-icon {
    font-size: 1.5rem;
    min-width: 2rem;
}

.security-content h3 {
    margin: 0 0 0.5rem 0;
    color: var(--color-primary);
    font-size: 1rem;
}

.security-content p {
    margin: 0;
    color: var(--color-gray);
    font-size: 0.875rem;
    line-height: 1.5;
}

.step-navigation-links {
    display: flex;
    justify-content: space-around;
    margin-bottom: 3rem;
}

.btn-outline {
    padding: 0.75rem 1.5rem;
    font-size: 0.9rem;
    border: 2px solid var(--color-primary);
    border-radius: 0.5rem;
    color: var(--color-primary);
    background-color: transparent;
    transition: all 0.3s ease;
    font-weight: 600;
}

.btn-outline:hover {
    background-color: var(--color-primary);
    color: white;
}

@media (max-width: 768px) {
    .payment-page {
        padding: 1rem;
    }
    
    .payment-grid {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .page-header h1 {
        font-size: 2rem;
    }
    
    .form-row {
        grid-template-columns: 1fr;
    }
    
    .payment-actions {
        flex-direction: column;
    }
    
    .mobile-options {
        flex-direction: column;
    }
    
    .security-grid {
        grid-template-columns: 1fr;
    }

    .steps-navigation {
        flex-direction: column;
        gap: 1rem;
    }
    
    .step-connector {
        width: 2px;
        height: 30px;
    }

    .step-navigation-links {
        flex-direction: column;
        gap: 1rem;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const paymentMethods = document.querySelectorAll('input[name="payment_method"]');
    const cardForm = document.getElementById('card-form');
    const transferInfo = document.getElementById('transfer-info');
    const mobileInfo = document.getElementById('mobile-info');
    
    paymentMethods.forEach(method => {
        method.addEventListener('change', function() {
            // Masquer tous les formulaires
            cardForm.style.display = 'none';
            transferInfo.style.display = 'none';
            mobileInfo.style.display = 'none';
            
            // Afficher le formulaire correspondant
            if (this.value === 'card') {
                cardForm.style.display = 'block';
            } else if (this.value === 'transfer') {
                transferInfo.style.display = 'block';
            } else if (this.value === 'mobile') {
                mobileInfo.style.display = 'block';
            }
        });
    });
    
    // Formatage du numéro de carte
    const cardNumber = document.getElementById('card_number');
    cardNumber.addEventListener('input', function() {
        let value = this.value.replace(/\s/g, '').replace(/\D/g, '');
        value = value.replace(/(\d{4})/g, '$1 ').trim();
        this.value = value;
    });
    
    // Formatage de la date d'expiration
    const expiryDate = document.getElementById('expiry_date');
    expiryDate.addEventListener('input', function() {
        let value = this.value.replace(/\D/g, '');
        if (value.length >= 2) {
            value = value.substring(0, 2) + '/' + value.substring(2, 4);
        }
        this.value = value;
    });
    
    // Formatage du CVV
    const cvv = document.getElementById('cvv');
    cvv.addEventListener('input', function() {
        this.value = this.value.replace(/\D/g, '').substring(0, 4);
    });
});
</script>
{% endblock %} 